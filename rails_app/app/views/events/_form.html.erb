<%= form_for(event) do |f| %>
  <% if event.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(event.errors.count, "error") %> prohibited this event from being saved:</h2>

      <ul>
      <% event.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :channel_id %>
    <%= f.text_field :channel_id %>
  </div>

  <div class="field">
    <%= f.label :name %>
    <%= f.text_field :name %>
  </div>

  <div class="field">
    <%= f.label :abstract %>
    <%= f.text_area :abstract %>
  </div>

  <div class="field">
    <%= f.label :zoom_url %>
    <%= f.text_area :zoom_url %>
  </div>

  <div class="field">
    <%= f.label :host_date %>
    <%= f.datetime_select :host_date %>
  </div>

  <div class="field">
    <%= f.label :from_date %>
    <%= f.datetime_select :from_date %>
  </div>

  <div class="field">
    <%= f.label :to_date %>
    <%= f.datetime_select :to_date %>
  </div>

  <div class="field">
    <%= f.label :is_delete %>
    <%= f.check_box :is_delete %>
  </div>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
